{{#switch sak.ytelsetype}}
    {{#case "ES"}}
    {{/case}}
    {{#case "FP" "SVP"}}
_Perioden fra og med {{periode.fom}} til og med {{periode.tom}}
    {{/case}}
    {{#case "OMP" "PSB" "PPN" "OLP" "FRISINN"}}
        {{#if delperioder}}
            {{#if (gt delperioder.length 2)}}
_Deler av perioden fra og med {{periode.fom}} til og med {{periode.tom}}
            {{else}}
_Gjelder {{formater-perioder delperioder}}
            {{/if}}
        {{else}}
_{{#if (eq periode.fom periode.tom)}}{{periode.fom}}{{else}}Perioden fra og med {{periode.fom}} til og med {{periode.tom}}{{/if}}
        {{/if}}
    {{/case}}
{{/switch}}