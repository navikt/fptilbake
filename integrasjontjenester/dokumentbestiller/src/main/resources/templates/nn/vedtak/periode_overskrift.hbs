{{#switch sak.ytelsetype}}
    {{#case "ES"}}
    {{/case}}
    {{#case "FP" "SVP"}}
_Perioden frå og med {{periode.fom}} til og med {{periode.tom}}
    {{/case}}
    {{#case "OMP" "PSB" "PPN" "OLP" "FRISINN"}}
        {{#if delperioder}}
            {{#if (gt delperioder.length 2)}}
_Delar av perioden frå og med {{periode.fom}} til og med {{periode.tom}}
            {{else}}
_{{formater-perioder delperioder fra="frå"}}
            {{/if}}
        {{else}}
_{{#if (eq periode.fom periode.tom)}}{{periode.fom}}{{else}}Perioden frå og med {{periode.fom}} til og med {{periode.tom}}{{/if}}
        {{/if}}
    {{/case}}
{{/switch}}