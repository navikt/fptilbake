<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <parent>
        <artifactId>integrasjontjenester</artifactId>
        <groupId>no.nav.foreldrepenger.tilbakekreving</groupId>
        <version>${revision}${sha1}${changelist}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>pdfgen</artifactId>
    <packaging>jar</packaging>
    <name>FPTILBAKE :: Integrasjontjenester - Pdfgen</name>

    <dependencies>
        <dependency>
            <groupId>com.openhtmltopdf</groupId>
            <artifactId>openhtmltopdf-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.openhtmltopdf</groupId>
            <artifactId>openhtmltopdf-pdfbox</artifactId>
            <exclusions>
                <exclusion>
                    <!-- openhtmltopdf-pdfbox er ikke enig med seg selv hvilken versjon av pdfbox som skal brukes, se under -->
                    <groupId>org.apache.pdfbox</groupId>
                    <artifactId>pdfbox</artifactId>
                </exclusion>
                <exclusion>
                    <!-- unngår commons-logging siden den forkludrer logging -->
                    <groupId>commons-logging</groupId>
                    <artifactId>commons-logging</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <!-- openhtmltopdf-pdfbox er ikke enig med seg selv hvilken versjon av pdfbox som skal brukes, tvinger denne (se dependency management i hoved-pom for versjon)-->
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <exclusions>
                <exclusion>
                    <!-- unngår commons-logging siden den forkludrer logging -->
                    <groupId>commons-logging</groupId>
                    <artifactId>commons-logging</artifactId>
                </exclusion>
                <exclusion>
                    <!-- TODO fjern denne overstyringen -->
                    <!-- setter eldre versjon for at det skal bygge med github actions, den klarer ikke laste ny versjon fra maven central -->
                    <groupId>org.apache.pdfbox</groupId>
                    <artifactId>xmpbox</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>com.openhtmltopdf</groupId>
            <artifactId>openhtmltopdf-svg-support</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>commons-logging</groupId>
                    <artifactId>commons-logging</artifactId>
                </exclusion>
                <!-- denne drar inn en annen versjon enn hva som brukes i openhtmltopdf-pdfbox -->
                <exclusion>
                    <groupId>org.apache.xmlgraphics</groupId>
                    <artifactId>xmlgraphics-commons</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>xml-apis</groupId>
                    <artifactId>xml-apis</artifactId>
                </exclusion>
                <exclusion>
                    <!-- TODO fjern denne overstyringen -->
                    <!-- setter eldre versjon for at det skal bygge med github actions, den klarer ikke laste ny versjon fra maven central -->
                    <groupId>org.apache.pdfbox</groupId>
                    <artifactId>xmpbox</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <!-- openhtmltopdf-svg-support er ikke enig med seg selv hvilken versjon av xml-apis som skal brukes, tvinger denne (se dependency management i hoved-pom for versjon) -->
            <groupId>xml-apis</groupId>
            <artifactId>xml-apis</artifactId>
        </dependency>
        <dependency>
            <!-- TODO fjern denne overstyringen -->
            <!-- setter eldre versjon for at det skal bygge med github actions, den klarer ikke laste ny versjon fra maven central -->
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>xmpbox</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>commons-logging</groupId>
                    <artifactId>commons-logging</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

    </dependencies>

</project>

